import './App.css'

export const Bubble = (props) => {

    return (
        <div className='link-bubble'>
        {/*
        <a href={props.url}>
            <img src={props.img} alt={props.name}/>
        </a>
        */}
        </div>
    )
    
}

